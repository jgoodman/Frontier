#!/usr/bin/perl
use strict;
use warnings;
use lib qw(../lib ./lib);

use Respite::CommandLine;
Respite::CommandLine->run({
    api_meta => {
        remote  => 1, # will use Repsite::Client
        service => 'frontier',
        host    => 'localhost',
        brand   => 'test',
        pass    => '-',
        port    => 50443,
    },
});

